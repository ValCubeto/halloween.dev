function findSafestPath(dream: number[][]): number {
  let danger = 0;
  for (let i = 1; i < dream[0].length; i += 1) {
    let path = dream[0].slice(0, i);
    for (let j = )
  }
  return 0;
}

/*
assertEq(
  findSafestPath([
    [1, 3, 1],
    [1, 5, 1],
    [4, 2, 1],
  ]),
  7
) */